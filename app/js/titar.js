//const path = require('path');

let titar = {
	controllers: __dirname + "/controllers/",
	models: __dirname + "/models/",
	views: __dirname + "/views/",
	libs: __dirname + "/libs/",
	base: __dirname + "/../../"
}

module.exports = titar;
